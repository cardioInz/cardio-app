<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="cardio_app.activity.statistics.StatisticsActivity">

    <ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:paddingTop="@dimen/activity_vertical_margin">

            <Space android:minHeight="5sp" android:layout_height="wrap_content" android:layout_width="wrap_content" />

            <Button android:text="@string/show_counter_statistics"
                android:id="@+id/show_counter_stats_btn"
                android:onClick="moveToActivity"/>

            <Space android:minHeight="10sp" android:layout_height="wrap_content" android:layout_width="wrap_content" />

            <Button android:text="@string/show_last_measurements_statistics"
                android:id="@+id/show_last_measurements_stats_btn"
                android:onClick="moveToActivity"/>

            <Space android:minHeight="10sp" android:layout_height="wrap_content" android:layout_width="wrap_content" />

            <Button android:text="@string/title_activity_create_pdf"
                android:id="@+id/create_pdf_btn"
                android:onClick="moveToActivity"/>

        </TableLayout>
    </ScrollView>


</android.support.design.widget.CoordinatorLayout>
